#include <iostream>
using namespace std;

int main()
{
    int num = 10; // Declare and initialize a variable

    // If condition to check if the number is positive
    if (num > 0)
    {
        cout << "Number is positive." << endl; // If true, print this
    }
    else
    {
        cout << "Number is not positive." << endl; // Otherwise, print this
    }
    return 0;
}
